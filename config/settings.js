/* ===========================================================
   DRAFTED APPAREL — Tunable business settings
   -----------------------------------------------------------
   Change these numbers here; nothing else needs editing.
   =========================================================== */

module.exports = {
  // Max FREE automated re-rolls a customer gets before the
  // hand-finish option unlocks. Change this one number to adjust.
  // (Each re-roll is a paid AI generation on your side, so mind cost.)
  REGEN_LIMIT: 3,
};
